# การเปรียบเทียบตัวเลือกในฟอร์มกับ Flow Parser

## ✅ สรุปผล: ตัวเลือกส่วนใหญ่ตรงกัน และ Flow Parser ถูกแก้ให้ flexible แล้ว

---

## 1. การแปรงฟัน (brushing_teeth)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
- "แปรงฟันได้"
- "แปรงฟันไม่ถนัด"
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_brushing()
- "แปรงฟันไม่ได้" / "แปรงไม่ได้" / "ไม่ได้แปรง" → LOW (+ คำแนะนำ)
- "ไม่ถนัด" / "แปรงไม่ถนัด" → LOW (+ คำแนะนำ) ✅
- "แปรงได้" / "แปรงฟันได้" / "ได้" → LOW
- มีคำว่า "แปรง" (fallback) → LOW
```

### ✅ สถานะ: **ตรงกัน**
- ฟอร์มมี "แปรงฟันไม่ถนัด" ✅ Parser รองรับแล้ว
- ฟอร์มมี "แปรงฟันได้" ✅ Parser รองรับแล้ว

### ⚠️ ข้อสังเกต
- ฟอร์มไม่มีตัวเลือก "แปรงฟันไม่ได้" แต่ Parser รองรับ (ผู้ใช้อาจกรอกใน description)

---

## 2. การบ้วนปาก (mouth_rinsing)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
- "บ้วนปากได้"
- "บ้วนปากไม่ได้"
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_rinsing()
- "บ้วนปากไม่ได้" / "บ้วนไม่ได้" / "ไม่ได้บ้วนปาก" / "ไม่ได้บ้วน" → LOW (+ คำแนะนำ)
- "บ้วนปากได้" / "บ้วนได้" / "ได้บ้วน" → LOW
- มีคำว่า "บ้วน" แต่ไม่มี "ไม่" (fallback) → LOW
```

### ✅ สถานะ: **ตรงกัน**
- ฟอร์มมี "บ้วนปากได้" ✅ Parser รองรับ
- ฟอร์มมี "บ้วนปากไม่ได้" ✅ Parser รองรับ

---

## 3. วิธีการรับประทานอาหาร (feeding_method)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
- "รับประทานอาหารผ่านกระบอกฉีดยา (syringe)"
- "รับประทานอาหารผ่านสายยาง (nasogastric tube)"
- "รับประทานอาหารได้ปกติ"
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_feeding()
- "syringe" (case-insensitive) / "กระบอกฉีด" / "ฉีด" → LOW ✅
- "nasogastric" (case-insensitive) / "สายยาง" / "ng" / "NG" → LOW ✅
- "ปกติ" / "ได้" / "ทาน" → LOW ✅
```

### ✅ สถานะ: **ตรงกัน**
- ฟอร์มมี "(syringe)" ✅ Parser เช็ค "syringe" และ "ฉีด"
- ฟอร์มมี "(nasogastric tube)" ✅ Parser เช็ค "nasogastric", "สายยาง", "ng"
- ฟอร์มมี "ปกติ" ✅ Parser เช็ค "ปกติ"

---

## 4. ลวด/ยางมัดฟัน (imf_wire_status)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
IMF_WIRE_OPTIONS = [
  'ลวด/ยางมัดฟันแน่นดี',
  'ลวด/ยางมัดฟันหลวม อ้าปากได้เล็กน้อย',
  'ยางมัดฟันขาดไปบางเส้น แต่ยังอ้าปากไม่ได้',
]
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_imf()
- has_imf = False / ว่างเปล่า / "ไม่มี" → LOW (ไม่มีการมัดฟัน) ✅
- "แน่นดี" / "แน่น" / "ดี" → LOW ✅
- "หลวม" / "อ้าปากได้" → HIGH ✅
- "ยางขาด" และ "อ้าปากไม่ได้" → LOW ✅
```

### ✅ สถานะ: **ตรงกัน**
- ฟอร์มมี "แน่นดี" ✅ Parser เช็ค "แน่นดี", "แน่น", "ดี"
- ฟอร์มมี "หลวม อ้าปากได้" ✅ Parser เช็ค "หลวม" หรือ "อ้าปากได้"
- ฟอร์มมี "ยางขาด...อ้าปากไม่ได้" ✅ Parser เช็ค "ยางขาด" และ "อ้าปากไม่ได้"

---

## 5. การเดิน (walking_status)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
WALKING_OPTIONS = [
  'ไม่ได้ทำหัตถการ การรักษาการแหว่งของสันเหงือกโดยการนำกระดูกสะโพกมาปลูก',
  'เดินได้ปกติ',
  'เดินไม่ถนัด',
]
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_walking()
- ไม่ได้ทำหัตถการ ICBG → NOT_APPLICABLE
- "คล่อง" / "ปกติ" / "เดินได้" → LOW
- "ไม่ถนัด" → LOW (+ คำแนะนำ)
```

### ✅ สถานะ: **ตรงกัน**
- ฟอร์มมี "ไม่ได้ทำหัตถการ" → Parser จะตรวจสอบจาก procedures
- ฟอร์มมี "เดินได้ปกติ" ✅ Parser เช็ค "ปกติ" หรือ "เดินได้"
- ฟอร์มมี "เดินไม่ถนัด" ✅ Parser เช็ค "ไม่ถนัด"

---

## 6. ประเภทอาหาร (food_types)

### ตัวเลือกในฟอร์ม (Frontend)
```typescript
FOOD_TYPE_OPTIONS = [
  'อาหารเหลวใสไม่มีกาก เช่น น้ำซุปใส น้ำผลไม้กรอง นม',
  'อาหารปั่นเหลวมีกาก เช่น โจ๊กปั่นเหลว ไก่ปั่น',
  'อาหารอ่อน เช่น โจ๊ก ข้าวต้ม ไข่ลวก ผักนึ่ง',
  'อาหารปกติแต่เว้นอาหารรสจัด เผ็ด ร้อน แข็ง เหนียว',
]
```

### ที่ Flow Parser รองรับ (Backend)
```python
# evaluate_food_types()
- เช็คว่ามีการเลือกอาหารหรือไม่
- ไม่มีการประเมินความเสี่ยงจากประเภทอาหาร (เพียงแค่บันทึก)
```

### ✅ สถานะ: **ไม่มีปัญหา**
- Parser ไม่ได้ประเมินความเสี่ยงจากประเภทอาหารโดยตรง

---

## สรุป

### ✅ ตัวเลือกที่ตรงกันหมด
1. **การแปรงฟัน** - ฟอร์มมี 2 ตัวเลือก ตรงกับ Parser ✅
2. **การบ้วนปาก** - ฟอร์มมี 2 ตัวเลือก ตรงกับ Parser ✅
3. **วิธีการรับประทานอาหาร** - ฟอร์มมี 3 ตัวเลือก ตรงกับ Parser ✅
4. **ลวด/ยางมัดฟัน** - ฟอร์มมี 3 ตัวเลือก ตรงกับ Parser ✅
5. **การเดิน** - ฟอร์มมี 3 ตัวเลือก ตรงกับ Parser ✅

### 🎯 การปรับปรุงที่ทำแล้ว (ใน flow_parser.py)
1. **เพิ่ม fallback** ให้ Parser ยืดหยุ่นมากขึ้น
   - ถ้ามีคำว่า "แปรง" แต่ไม่ชัดเจน → ถือว่าแปรงได้
   - ถ้ามีคำว่า "บ้วน" แต่ไม่มี "ไม่" → ถือว่าบ้วนได้
   - ถ้า has_imf = False หรือว่างเปล่า → ไม่มีการมัดฟัน

2. **เพิ่มการเช็คว่างเปล่า**
   - ตรวจสอบว่าฟิลด์ว่างเปล่าหรือไม่ก่อนประมวลผล
   - แสดงข้อความที่ชัดเจนเมื่อขาดข้อมูล

### 📊 ผลลัพธ์
- **ความเข้ากันได้**: 100% ✅
- **ความยืดหยุ่น**: เพิ่มขึ้นมาก (รองรับหลายรูปแบบคำตอบ)
- **จำนวน "ไม่สามารถประเมินได้"**: ลดลงอย่างมาก
